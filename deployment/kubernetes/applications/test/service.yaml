apiVersion: "v1"
kind: "Service"
metadata:
  name: "curl-test"
  labels:
    app: "curl-test"
spec:
  selector:
    app: "curl-test"
  ports:
    - name: "http"
      port: 8080
      protocol: "TCP"
      targetPort: 8080
      appProtocol: http
  sessionAffinity: "None"
  type: "ClusterIP"
