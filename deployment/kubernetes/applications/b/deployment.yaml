apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "b"
  labels:
    app: "b"
spec:
  selector:
    matchLabels:
      app: "b"
  revisionHistoryLimit: 10
  replicas: 1
  template:
    metadata:
      labels:
        app: "b"
        sidecar.istio.io/inject: "true"
    spec:
      containers:
        - imagePullPolicy: "IfNotPresent"
          image: "b"
          ports:
            - name: "http"
              containerPort: 8080
          name: "b"
          env:
            - name: PORT
              value: "8080"
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: "RollingUpdate"

---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "b"
  labels:
    app: "b"
spec:
  selector:
    app: "b"
  ports:
    - name: "http"
      port: 8080
      protocol: "TCP"
      targetPort: 8080
      appProtocol: http
  sessionAffinity: "None"
  type: "ClusterIP"
